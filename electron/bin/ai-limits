#!/usr/bin/env bash
export ELECTRON_RUN_AS_NODE=1
APP_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ -f "$APP_DIR/resources/app.asar" ]; then
  exec "$APP_DIR/QuotaLens.exe" "$APP_DIR/resources/app.asar/dist/cli/index.js" "$@"
else
  exec "$APP_DIR/QuotaLens.exe" "$@"
fi
